WXAgent 是一个桌面应用（或 Web 应用）式的工具，左侧是固定导航栏，右侧是随页面切换的主工作区。所有页面都遵守同一套布局与交互风格，方便 Codex 直接按模块实现。


注意，我这里是需要在WXAgent中完成全流程，使用go/flutter后端进行处理，不要额外添加echotrace或者wx_key这些开源app。我想要用自己重新写的代码实现全流程

一、全局布局与导航

左侧固定侧边栏
顶部：
LOGO（小图标），在WXAgent_logo中
右侧或下方显示产品名：WXAgent
导航菜单（竖直排列）：
密钥获取
聊天记录读取与导出
聊天记录分析
聊天记录 AI 总结
设置
当前页面高亮显示（例如背景色 + 竖线标记）。

点击菜单项时，右侧主内容区域切换到对应页面。

全局约束：界面上任何位置只出现“WXAgent”这个产品名，不能出现 echotrace 或 wx_key 等内部实现名。它们只在代码层面作为依赖参考。



右侧主内容区域

顶部可选一个页标题 + 简短说明文字。

下方是该功能对应的表单、表格、按钮和结果展示区域。

所有页面统一使用基础按钮样式 / 表单样式（便于 Codex 共用组件）。

二、页面 0：欢迎页 & 新手指引

触发逻辑
启动应用时，如果检测到尚未完成关键配置（比如没有密钥、没有聊天数据库路径），默认进入欢迎页。
否则可以直接进入上次使用的功能页。

布局结构
标题区
大标题：WELCOME! 或 欢迎使用 WXAgent
副标题：简短一句话介绍产品
“WXAgent 帮助你解密、导出并分析微信聊天记录，还能用 AI 进行总结。”

产品介绍区
用简单几行文字说明：
本地的，帮助大家读取自己的聊天记录并进行总结
加上一些法律上的免责条款
功能：密钥获取、聊天记录读取与导出、聊天记录分析、AI 总结等。

新手指引区
用一个步骤列表展示使用流程示意，比如：
在 「密钥获取」 页面中读取微信聊天记录密钥
在 「聊天记录读取与导出」 页面中批量解密并更新聊天数据库
在 「聊天记录分析」 页面中查看聊天统计与分析结果
在 「聊天记录 AI 总结」 页面中选择时间段，获取 AI 总结

每一步可以附一个“去设置”按钮或跳转链接。

作者联系方式
一块简单的信息卡片：
“作者联系方式：xuang_work@foxmail.com，Github@billyoftea”


三、页面 1：密钥获取

逻辑上参考 wx_key 的实现，但在 UI 中只称为“密钥获取”。

主要目标
使用 hook 技术从本地微信环境中读取聊天记录的密钥（文字 key）。
显示读取结果，并提供复制功能。
布局与组件
标题：密钥获取
说明区域
简短说明密钥的作用和安全性：
“WXAgent 需要读取你的微信聊天记录密钥，用于解密本地聊天数据库。”
“密钥只保存在本地，不会上传到服务器。”

微信安装 / 数据路径信息（可选）
显示检测到的微信安装路径或数据路径（只读文本 + “更改”按钮）。
若未检测到，显示错误提示，并提供输入 / 选择路径控件（文件夹选择器）。

操作区
主按钮：开始获取密钥 / 读取密钥
点击后：
显示加载中的状态（例如进度圈）。
若成功，进入“结果区域”显示密钥。
若失败，显示错误提示（可能包括建议：检查微信是否安装、是否已登录等）。
结果区域
标题：“密钥读取结果”
只读文本框：
内容为获取到的 key（字符串）。

按钮：
复制密钥：复制到剪贴板

状态信息：
成功时间戳，例如“最后更新：2025-xx-xx xx:xx”
错误信息区域（当读取失败时替换显示）。

四、页面 2：聊天记录读取与导出

逻辑参考 echotrace 中的数据管理、导出记录两部分功能。

主要目标
支持对微信聊天数据库文件的批量解密和增量更新。
支持将聊天记录导出到本地文件。
首次读取时默认全部解密。
没有用户明确同意前，不执行导出操作。

布局与组件
整个页面可以分成上下两个大块：
区块 A：聊天记录读取 & 解密

配置区域
字段：聊天数据库路径
文本框（显示当前路径）
选择文件夹 按钮（打开系统文件夹选择器）
可选：勾选项
首次使用，进行全量解密
以后默认进行增量更新
数据库文件列表区域
使用表格展示已发现的数据库文件：

列示例：

选择框（多选）
文件名
大小
解密状态（未解密 / 已解密 / 部分更新）
最后解密时间
顶部操作按钮：
批量解密：对选中项或全部执行解密
增量更新：对已解密数据库执行增量解密 / 更新

状态与进度
进度条 / 状态文本：
“正在解密 xx.db (2/10)”
完成后显示简短提示：
“解密完成，共处理 X 个数据库。”

区块 B：聊天记录导出
过滤条件
聊天对象选择：
下拉框 / 搜索型选择器（联系人或群列表）。
时间范围选择：
起始日期、结束日期控件。
导出设置

导出路径（文件夹选择器）

导出格式只实现json格式

确认勾选框：
我已知晓并同意将选定聊天记录导出到本地文件
未勾选时，“导出”按钮禁用，确保“没有用户允许，不许导出”。

导出操作
主按钮：导出聊天记录
点击后：
检查是否已完成必要配置（密钥、路径、勾选许可）。
显示进度条。
完成后显示导出结果信息：导出的文件路径、导出条数等。

五、页面 3：聊天记录分析

功能类似 echotrace 的分析模块，但以 WXAgent 品牌呈现。

主要目标
对已解密的聊天记录进行可视化分析，提供统计与图表。
布局与组件

时间范围选择（开始日期、结束日期）。

对于单个联系人，总结：
展示私聊的总体统计信息，包括总消息数、活跃天数、平均每天聊天时长、首条消息时间和最新消息时间。
呈现消息类型的分布情况，包含文本、图片、语音、视频和其他类型消息的数量及占比。
显示发送和接收消息的比例及具体数量。
提供聊天最多的联系人 Top 10 的查看入口，且可切换查看 Top 20、Top 50 的联系人。

对于群：
    总消息数
    活跃天数
    平均每天消息数
发送 / 接收 / 互动比例
    群内成员发送消息总数及占比
    群内成员接收消息总数及占比
    互动类数据（如消息被回复数、被点赞数等，若支持）及占比
群成员互动统计
    聊天最活跃的群成员 Top N（可设置 Top 10、Top 20 等）
    群成员发言频率分布（如日均发言数、周发言天数等）


六、页面 4：聊天记录 AI 总结

在这里调用 wx_agent 中的 Python 实现方法。

主要目标

用户选择时间范围后，默认使用预设“总结 prompt”，生成聊天记录摘要。
同时提供自定义提示词，让用户用自己的 prompt 调用 AI。

布局与组件
过滤条件
聊天对象选择（与分析页类似）。
时间范围选择

提示词区域

单选按钮：
使用默认总结提示词
使用自定义提示词
当选择自定义时：
显示多行文本框：自定义提示词
用户可以输入例如：“梳理这段时间和AI相关的信息”

操作按钮
主按钮：开始总结
点击后：
发送请求到后台（调用 wx_agent 的总结逻辑）。
显示加载动画。


4. 总结结果展示区域（含下拉框）
4.1 结果选择下拉框（新增）

用于展示本地保存的总结结果文件，例如（按时间命名）：

2024-01-01_2024-01-07_总结.md

2024-01-08_2024-01-14_总结.md

…

下拉框功能：

显示：本地保存的所有总结记录（按时间排序）

点击某个记录后，即刻在下面的结果区展示对应内容

下拉框上方可以有小标题：
“查看历史总结结果”

4.2 当前结果展示区

多行可滚动文本框（高度固定，内部滚动）

用于展示：

当前生成的新总结内容，或

用户从下拉框中选择的历史总结内容

4.3 操作按钮（与展示区域相关）

复制总结内容

保存为 Markdown 文件

保存为 TXT

可选：在文件夹中打开（定位到存放总结的目录）


七、页面 5：设置

设置聊天记录 JSON 输出 path 与大模型 API 等。


主要目标
配置路径与模型参数，使其它页面可以正常运行。

布局与组件
路径设置区
字段：聊天记录 JSON 输出路径
总结输出路径

文本框（当前路径）
选择文件夹 按钮
可选：聊天数据库路径

文本框 + 选择文件夹按钮（如果没在前面页面统一设置的话）
大模型 API 设置区
字段：

API Base URL

API Key

模型名称（如 gpt-x / 其他）

加入提示：需要根据api配置规范设置

测试连接 按钮：
点击后尝试发起一个简单请求，成功 / 失败给予提示。

