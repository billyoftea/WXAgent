现在我想要：
    1.集成 go_decrypt，不使用GUI，直接将其代码化集成为后端
        测试：cd C:\Users\Lenovo\Desktop\WXAgent\backend


        # 1. 修改代码后，必须先编译
        go build -o wxagent_backend.exe ./cmd/wxagent_backend

        # 2. 编译成功后，才能运行新版本
        .\wxagent_backend.exe export-auto

        # 方式1: 最简单（全自动）
        .\wxagent_backend.exe export-auto

        # 方式2: 指定目录
        .\wxagent_backend.exe export-auto --wechat-dir "C:\Users\YourName\Documents\WeChat Files\wxid_xxx"

        # 方式3: 指定日期范围
        .\wxagent_backend.exe export-auto --start-date 2025-12-01 --end-date 2025-12-05

        export-auto 命令参数
            可用参数：
            --wechat-dir (可选)

            说明：指定微信数据目录路径
            默认值：空（自动检测）
            示例：--wechat-dir "C:\Users\Lenovo\Documents\xwechat_files\wxid_xxx"
            --start-date (可选)

            说明：指定导出的开始日期
            格式：YYYY-MM-DD
            默认值：空（启用增量导出模式）
            示例：--start-date 2025-12-01
            --end-date (可选)

            说明：指定导出的结束日期
            格式：YYYY-MM-DD
            默认值：今天
            示例：--end-date 2025-12-05
    

2. json进一步解析：
    1.现在json中还是有一些乱码：
    按照如下的规则替换字符串：
            "type": "系统消息",str_content替换为[不支持的消息类型]
            "type": "表情",str_content替换为[表情消息]
            "type": "未知消息类型(227633266737)",看看echotrace是如何解析，将其str解析为#接龙\n确认明下午来一起玩玩玩🥳\n例 姓名-部门-年级\n\n1. 匡浩鑫-综合事务部-25\n2. 任凯-主席团-24\n3. 李昱铭-综合事务部-25\n4. 曾瑞云 -文体部-25\n5. 王祥宇-主席团-24\n6. 张豪乐-艺术团&媒体运营部-25\n7. 刘耀旸-文体-25\n8. 张壹豪-社团服务部-25\n9. 祝焱炎-文体-25"的，你也要这么做。

    2.你需要看看echotrace的源代码，是如何对json中的各种信息进行解析的，避免乱码


3. api集成
    现在已经配置成功了deepseek
    api:sk-70b3bb08f690485b85bce660f6ec32fc
    测试：go build -o test_deepseek.exe .\cmd\test_deepseek

    我现在想要把后端整合，先对json进行解析和拼接，获得某个时间段的所有内容

    工作流程为：
        1.把json全部合并解析为消息列表文件
        2.对消息列表文件按照时间进行切片，选出某些天的消息，并对其进行字符串拼接，拼接格式为：时间，发送者，消息内容
        3.考虑到deepseek的上下文限制，我想要把消息列表文件再进行分段（由于deepseek的上下文限制为128K，我想要调用token计数，每个），分别进行总结
        4.总结的prompt:
            """请总结以下微信群聊天记录的主要内容和讨论话题：

            {merged_content}

            要求：
            1. 用中文总结
            2. 总结出主要话题和讨论内容
            3. 提取出重要信息（如招聘信息、活动信息等）
            4. 概括参与者的主要观点或反应"""
        5.把所有的总结进行合并，形成最终的总结报告


新的测试：
# Terminal 1 - 启动后端
cd C:\Users\Lenovo\Desktop\WXAgent\backend
go run cmd/wxagent_backend/main.go server

# Terminal 2 - 启动前端
cd C:\Users\Lenovo\Desktop\WXAgent\frontend
flutter pub get
flutter run -d windows


exe打包方案？
cd scripts
cd C:\Users\Lenovo\Desktop\WXAgent\scripts; .\build_release.bat


wxagent不如echotrace导出的聊天记录全？？？




帮我修改前端实现：
1.在密钥获取功能中，删除”打开密钥文件“选项
2.删除”微信环境状态“这个block。
3.删title: '缺少密钥时自动启动 wx_key')'开启后一键导出会尝试注入密钥，再执行解密'),   这个功能  在聊天记录导出这个前端只保留一键解密+导出 
4.在导出会话列表里面，现在打开json错位，查看状态是灰色的而且错位。类别没有正确显示。将“上次导出”栏目删掉。


1.现在前端字体是怎么实现的？
    现在字体粗细还是不一样，帮我修复
3.导出聊天记录的时候将后端的这些输出也在前端展示，让用户知道这在工作
  [DEBUG] 当前用户 wxid_t0lk4ald194u22 在 Name2Id 中的 rowid: 2
  [OK] Exported wxid_72rns54z1w7522: 31 messages
  [DEBUG] 当前用户 wxid_t0lk4ald194u22 在 Name2Id 中的 rowid: 2
  [MERGE] wxid_1zxea9rhgt4a12: 14 existing + 86 new = 100 total
4.删除欢迎页的当前状态
5.ai总结部分，应该是有两个prompt的，一个是分chunk的，一个是最终总结的。在使用默认提示词的时候，展示这两个的prompt（只读状态，用户不能修改），在自定义提示词的时候，展示两栏供用户修改
6.在已经修改了output_dir，（一个放入chat_history文件夹，一个放入analysis文件夹），更新聊天记录分析时使用的路径
7.聊天记录ai总结部分，当前总结/历史记录这个下拉栏一直没有正常？我本地已经有了md文件，但是下拉栏没有显示出来
